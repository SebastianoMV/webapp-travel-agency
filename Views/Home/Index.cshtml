
@{
    ViewData["Title"] = "Home Page";
}
@model List<Package>

<div class="text-center">
    <a class="btn btn-primary" href="@Url.Action("Create")">Crea nuovo viaggio</a>
    @if(Model.Count >0){

        <div class="row">
        
            @foreach(Package package in Model){


                <div class="col-sm-6">
                    <div class="card mb-3">
                        <div class="card-body">
                            <img class="card-img" src="@package.Image">
                            <h5 class="card-title">@package.Name</h5>
                            <p class="">@package.Description</p>
                            <p class="">@package.Price &euro;</p>
                            <p class="">Viaggio di @package.Days giorni</p>
                            <a href="@Url.Action("Details", "Home", new {Id= package.Id})" class="btn btn-primary">Dettagli</a>
                            <a href="@Url.Action("Update", "Home", new {Id= package.Id})" class="btn btn-primary">Modifica</a>
                            <form asp-controller="Home" asp-action="Delete" asp-route-id="@package.Id" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-outline-danger">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>

            }
        </div>

    }
    else{
        <h1> Non ci sono viaggi </h1>
    }
</div>
